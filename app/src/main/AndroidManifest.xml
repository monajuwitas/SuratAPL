<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.monajuwitas.suratapl">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplahsTTEActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"/>
        <activity
            android:name=".Eselon.HomeActivity"
            android:label="@string/title_activity_home" />
        <activity android:name=".PDFsptActivity" />
        <activity android:name=".PDFndActivity" />
        <activity android:name=".PDFeofficeActivity" />
        <activity android:name=".Gubernur.HomeGubActivity" />
        <activity android:name=".Asisten.HomeAsistenActivity" />
        <activity android:name=".Sekda.HomeSekdaActivity" />
    </application>

</manifest>